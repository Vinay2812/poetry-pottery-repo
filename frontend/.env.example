# Where the browser and the server components send GraphQL
NEXT_PUBLIC_API_URL=http://localhost:6060/graphql

NEXT_PUBLIC_LOG_LEVEL=debug
LOG_LEVEL=debug

CLERK_SECRET_KEY=sk_test_xxx
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_xxx

# Codegen only: point SCHEMA_URL at ../api/schema.gql to run it without the API up.
# SCHEMA_SYNC_KEY must match the API's when introspecting a deployed endpoint.
SCHEMA_URL=http://localhost:6060/graphql
SCHEMA_SYNC_KEY=change-me
